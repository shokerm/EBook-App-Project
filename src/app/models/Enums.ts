export enum AuthLevel {
    visitor = 0,
    user = 1,
    admin = 2
}

export enum LocalStorageKey {
    id = "id",
    token = "token",
    refershToken = "refreshToken",
    currentUserName = "username",
    isLogin = "isLogin"
}